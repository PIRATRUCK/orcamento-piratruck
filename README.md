# Sistema de Orçamento — PIRATRUCK / EJR

Orçamento de peças (PIRATRUCK) e serviços (EJR) com geração de PDF (Imprimir/PDF) e **histórico sincronizado via Firebase** (coleção `orcamentos`).

- Login da equipe: **PIRATRUCK** / senha **PRTK2026** (trocável no código: `TEAM_USER` / `TEAM_PASSWORD`).
- Abre já conectado (config embutida). Todos que abrirem veem o mesmo histórico.

Para usar: baixe `Orcamento-Piratruck-EJR.html` e abra no navegador.

> ⚠️ Firestore em modo de teste (aberto). Antes de hospedar público: Firebase Authentication + regras (dados de cliente/valores — LGPD).
